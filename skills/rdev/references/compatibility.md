# rdev compatibility reference

Use `rdev compat` before protocol or release changes. Agent uploads are governed
by the administrator release policy; unsigned development artifacts require an
explicit dev policy opt-in. `rdev agent status|plan HOST` is read-only and may
report unknown for fields the current protocol cannot observe.
